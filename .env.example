# Optional — only needed for the `synthesize` command
# Install with: pip install claude-session-index[synthesis]
ANTHROPIC_API_KEY=

# Optional — override default paths
# SESSION_INDEX_PROJECTS=~/.claude/projects
# SESSION_INDEX_DB=~/.session-index/sessions.db
# SESSION_INDEX_TOPICS=~/.claude/session-topics
